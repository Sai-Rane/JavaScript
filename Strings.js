console.log("Hello");
const firstName = "John";
const job = "programmer";
const birthYear = 1990;
const jonas = "I'm" + " " + firstName + " a " + birthYear + " " + job;
console.log(jonas);

//Template Literal example is shown below
// We need to use backtick to use template literal
console.log(`Just a regular String`);
const jonasNew = `I'm ${firstName} a ${birthYear} ${job}`;
console.log(jonasNew);

/*
-> DOM is a tree like structure made of nodes
-> Allows us to make JS interact with browser
-> We can write JS to create, modify and delete HTML ElementInternals, set styles classes and attributes and listen and respond to EventSource
-> DOM treee is generated from an HTML document, which we can then interact with
-> DOM is a very complex API(Application Programming Interface) that contains lot of methods and properties to interact with the DOM tree
*/
